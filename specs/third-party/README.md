# Third-Party OpenAPI Specs

Drop external provider OpenAPI files here (`*.yaml` or `*.yml`).

Then run:

```bash
pnpm gen:openapi
```

Generated TypeScript contracts will be written to:

`packages/api-client/src/generated`
