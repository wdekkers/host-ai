{
  "name": "@walt/service-messaging",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'No tests yet for @walt/service-messaging'"
  },
  "dependencies": {
    "@walt/contracts": "workspace:*",
    "@walt/db": "workspace:*",
    "libphonenumber-js": "^1.11.12",
    "twilio": "^5.5.1",
    "uuid": "^11.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
